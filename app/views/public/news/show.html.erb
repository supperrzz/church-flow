<p>
  <% if @article.image.present? %>
    <%= image_tag(@article.image.url, height: 100) %>
  <% end %>
</p>
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<%= link_to 'Back', public_news_path %>
