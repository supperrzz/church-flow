<div class="row">
  <div class="col-12">
    <%= form_with(model: admin_media_sermon, local: true) do |form| %>

      <div class="form-group">
        <%= form.label :video %>
        <% if admin_media_sermon.video.attached? %>
          <%= video_tag(url_for(admin_media_sermon.video), height: 300) %>
        <% end %>
        <%= form.file_field :video, accept: 'video/*'  %>
      </div>

      <div class="form-group">
        <%= form.label :title %>
        <%= form.text_field :title, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= form.label :speaker %>
        <%= form.text_field :speaker, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= form.label :scripture %>
        <%= form.text_field :scripture, class: 'form-control' %>
      </div>

      <div class="actions">
        <%= form.submit 'Save', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
