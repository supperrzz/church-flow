<div class="topnav navbar navbar-expand shadow navbar-light d-md-none">
  <div class="card-header d-flex rounded-0  justify-content-center align-items-center">
      <a href="/" class="h3 text-center text-white text-decoration-none m-0 logo">ChurchFlow</a><span class="badge badge-pill badge-info ml-1">beta</span>
  </div>
  <button type="button" class="btn btn-primary bg-white text-body ml-auto collapsed" data-toggle="collapse" data-target="#sidenav-outer" aria-expanded="false"><i class="fas fa-bars"></i></button>
</div>
<header class="page-header header-bg pb-20">
  <div class="container">
    <div class="page-header-content py-4">
      <div class="row align-items-center justify-content-between">
        <div class="col-auto">
          <h1 class="h2 page-header-title text-white">
            <% if local_assigns[:icon] != ''  %>
              <div class="page-header-icon">
                <i class="fas fa-xs <%= icon %> nav-icon mr-1"></i>
              </div>
            <% end %>
            <%= title %>
          </h1>
          <% if local_assigns[:subtitle]  %>
          <div class="page-header-subtitle"><%= subtitle %></div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</header>
